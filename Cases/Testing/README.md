# Testing a policy

Pure policy objects are easy to test because they have no clock, network, database, or static state. Useful cases include the minimum boundary, every missing rule, and a valid input with extra characters.
