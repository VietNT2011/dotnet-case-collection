# Options pattern

Bind related configuration into a typed options class instead of reading string keys throughout the application. Validate required values at startup so configuration errors fail early.
