# .NET case collection

Focused examples for reviewing common .NET application patterns. Each folder under `Cases` explains one idea and keeps the sample small enough to read in a single sitting.

- Options pattern and configuration validation
- Dependency injection and replaceable adapters
- Cooperative cancellation
- Explicit result values
- Input validation
- Small unit-testable policies

The existing JWT Clean Architecture sample remains a separate, larger case.
